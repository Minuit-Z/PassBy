<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 按下 -->
    <item android:drawable="@drawable/btn_green" android:state_pressed="true"/>
    <!-- 不可用 -->
    <item android:drawable="@drawable/btn_blue" android:state_enabled="false"/>
    <!-- 默认时 -->
    <item android:drawable="@drawable/btn_bg1"/>

</selector>